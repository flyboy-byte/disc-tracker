{
  "name": "disc-tracker",
  "private": true,
  "version": "1.0.0",
  "description": "Dev-only tooling for disc-tracker (the website itself has no build step or JS dependencies). Browser-level smoke tests for the Jinja/vanilla-JS frontend.",
  "scripts": {
    "test:ui": "playwright test",
    "test:physics": "node static/physics.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
